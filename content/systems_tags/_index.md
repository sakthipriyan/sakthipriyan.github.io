---
title: "systems_tags"
# Empty stub page. The browsable tag pages live at /<section>/tags/ (see the
# permalinks block in config.yaml), so this default taxonomy list has no content
# and no audience -- suppress it rather than publishing an empty page + feed.
build:
  render: never
  list: never
---
